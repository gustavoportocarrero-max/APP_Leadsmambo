# Permisos/ajustes locales de Claude Code (específicos de la máquina)
.claude/settings.local.json

# Config local de Supabase (claves) — en prod va por variables de entorno.
# Solo el config.js en la raíz de la app; NO afecta a api/config.js (serverless).
/apps/pipeline/config.js

# Dependencias / build (por si más adelante se agrega tooling)
node_modules/
dist/
.vercel/

# Sistema
.DS_Store
Thumbs.db
*.log
